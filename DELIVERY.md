# 本次交付

已接管现有网站，保留研究数据、军事分项、解释和部署工作流。2026-09-23 修复 Windows 默认中文编码导致的构建失败，完整 34 项测试通过。当前通过经用户授权的 GitHub 官方 CLI 发布；最终状态见 `docs/deployment-status.md`。

2026-09-23 修订：修复军事复合记录的 24 条浮点尾差展示，将五组记录拆成十个显示列；补入 8 个领域及 149 个字段对应的 PDF 解释与页码；验证只替换一个 Excel 即可更新全部数据。独立验收及最终本地全套 **34 项测试通过**。`dist/offline.html` 现为独立文件，内嵌数据和下载内容。

## 查看网站

解压交付包后，打开 `dist/offline.html`。这是使用同一份网站源码和数据生成的离线入口；该文件已内嵌数据与下载内容，无需保留相邻目录。正式线上版本入口为 `dist/index.html`。

网站包括首页、综合排名、完整数据表、65 个实体档案、2–5 个实体比较、指标目录和详情、来源方法、Excel/CSV/JSON 下载。支持搜索、排序、冻结、字段选择和原值/公式溯源。

## 数据和验收

- 9 张表，65 个实体；51 个正式排名，14 个补充记录；8 个领域。
- 221 个领域数据字段，其中 209 个具名字段、12 个无名辅助字段；不能当作独立研究指标数量。
- 独立核对 11,026 个存储单元格；1,891 条公式缓存全部与复算结果一致。
- 34 项本地自动测试通过，涵盖保真、发布阻断条件、排序、空值、转义、字段顺序、复合记录含义、单文件预览及更新联动。
- 原表既定名次、数值和研究判断未被网站改写。公开工作簿只清除身份及打印机元数据。

本轮已使用真实 Chromium 浏览器在本地 `/National-Power/` 子路径验证主要桌面与手机页面、搜索、军事分项溯源、比较和下载。Actions 与 Pages 线上验收待本次提交部署后执行；不会以本地通过替代线上验收。

## 需要作者关注的原表问题

能源 10 个除零错误；既定名次与公式参考值存在 15 处相邻逆序；参考值可超过 100；多数逐字段单位、年份、来源未注明；两列同名“铝”；军事多项复合编码；农业水源自给率名称与算式需复核；能源石油综合存在两种公式。全部保留原值，详细位置和解释见 `docs/qa/excel-audit.md`。

## 更新和上线

核心输入为 `data/source/national-power.xlsx`。以后保持表结构更新此文件即可重新生成；方法或结构变化需先阅读校验报告。`README.md` 列出了单命令导入、构建、预览和更新方法。

远端已有初始提交 `fa6a0aee2d5bf3bee3a21a2ff9177f165fd10912`，仅含空白 README；本次保留该历史。插件实际写入仍为 403，但用户已完成官方 CLI 设备授权。API 已确认 Pages 的 `build_type` 为 `workflow`，无需重复设置。

部署后目标地址为 `https://lawrence35952563.github.io/National-Power/`，现在未作为已上线地址提供。

详细验收状态见 `docs/acceptance-review.md`，权限证据见 `docs/deployment-status.md`。

本轮读数含义、PDF 映射和更新流程的复核见 `docs/qa/reader-semantics.md`。其中 `.1` 等可能的特殊标记和已丢失末尾零仍需作者判断；网页不自行恢复或推定装备数量。
