# 表格与导航交互验收 · 2026-09-24

## 当前状态

**已部署并实际验证。** [网站](https://lawrence35952563.github.io/National-Power/) · [功能提交 8f7aeb7](https://github.com/Lawrence35952563/National-Power/commit/8f7aeb7c111a594fc59c32927de59639fe3a5ef7) · [成功 Actions 35964560878](https://github.com/Lawrence35952563/National-Power/actions/runs/35964560878)。本地与 GitHub Actions 的 50 项测试通过，真实 Chromium 桌面/手机及线上检查通过。本记录只描述展示和交互，不新增研究数据审计结论。

## 基线与修改边界

- 从远端 main `d201030ddcec4dfeaaac1e7ca953d4c947162b6b` 开始，逐个核对 32 个跟踪文件与远端一致，未用旧 ZIP 覆盖工程。
- 未修改源 Excel、config、reading-guide.js、数据处理脚本、格式化规则或研究解释。主题列和国家页主题分组直接复用现有 facets，不新增研究主题。
- Excel SHA-256 仍为 `330dc73f643c117e05dfcd3f0ec8c419e2ea251025c2bbd2d7223e81a25b832a`。
- 原 42 项测试保留，新增 8 项 URL 状态、选项约束、主题列及历史快照测试。构建成功，50 项测试全部通过；既有数据检查仍随构建运行。

## 真实浏览器通过项目

浏览器为 Codex 内置 Chromium；桌面 1440×960，手机视口 390×844。实际执行输入、点击、选择、键盘操作、前进后退和刷新；截图仅辅助观察，不代替交互。

| 项目 | 实际操作与结果 |
| --- | --- |
| 排名往返 | 搜索日本、分类二流强国上、领域总览、军事排序、独立公式参考开关，进入日本后返回：条件及视图恢复；前进后退、即时刷新仍保留查询。手机再次执行，页面纵向位置与表格横向位置一并恢复。 |
| 排名比较篮 | 从日本、加拿大行内加入并打开两实体比较；保留数量上限和移除操作。 |
| 比较 | 添加中国、欧盟、肯尼亚达到五实体，上限禁用添加；移除加拿大恢复可添加。总览为综合名次与八领域结果，三项名次/五项评分分别分组；公式参考独立折叠。 |
| 多字段 | 农业工作表、既有主题筛选，保留谷物产量吨、年人均谷物和水源自给率；换主题仍保留已选字段。复制链接并刷新，四实体及三个字段恢复；手机打开同一链接恢复选择并可移除实体、切换工作表。 |
| 宽表 | 农业切换“总供给与人均基础”，列搜索“水”并添加水源自给率，按年人均谷物排序、搜索日本；详情返回后查询、排序、六列、自定义主题、列搜索、展开与纵向位置均恢复。 |
| 完整模式与冻结 | 恢复完整农业表得到全部 25 列及辅助行；实际横向滚动并进入详情返回，表格横向及纵向位置恢复，冻结实体列/表头继续使用 sticky。数值可用键盘打开溯源并跳转字段页，弹窗随跳转关闭。 |
| 手机宽表 | 能源主题“储量与当前开采速度”，列搜索“石油”，加入石油产量并隐藏石油储量，搜索欧盟、排序、进入欧盟详情正常。横向滚动限制在表格区域，页面无意外横向溢出。 |
| 国家页 | 检查日本、欧盟、肯尼亚；综合名次和八领域集中展示，补充记录提示保留。选择农业/政治定位，既有主题、其他记录、未记录/辅助字段及详细说明保留入口。手机领域定位栏随滚动可见，返回结构可用。字段页返回后领域展开和阅读位置恢复。 |
| 字段查找 | 指标详情搜索日本、从大到小、仅有记录项，进入国家再返回均恢复；目录搜索热量并筛选农业，进入每日热量再返回，条件恢复。 |
| 键盘 | 排名与宽表排序、行内比较、数值溯源使用键盘；焦点轮廓清楚，排序和加入/移除后留在对应按钮。手机菜单可以展开并导航。 |

## 检查中修复的问题

排名表切换/排序重绘后原按钮会被替换，导致键盘焦点丢失。已在排序和行内比较更新后将焦点恢复到对应按钮，并在真实手机视口使用 Enter 复验。搜索丢失的原问题由 URL 状态与按历史访问保存的界面快照共同解决。

## 状态约定

筛选、排序、主题、自定义列和比较选择进入网址。滚动、展开内容及焦点仅存在当前标签页会话的最近 40 次历史访问中，不包含研究数据。浏览器返回恢复该次访问，顶部导航进入该页面默认视图；新打开的分享链接恢复选择，从相应页面起点阅读。国家链接可携带领域定位。

## 实际线上验收

- Actions 35964560878 的 build、deploy 均成功；已检查 build 日志，Python 3.12.14 下显示 `Ran 50 tests`、`OK`。工作流和 Pages Source 沿用现有配置。
- 23 个实际 HTTP 请求全部 200，涵盖根目录、脚本、样式、数据、离线页、下载及带内容哈希的资源地址。线上脚本和样式与本地源码一致，下载文件与同源构建结果一致，源 Excel 指纹未变。
- 在线手机视口执行日本搜索、领域总览、经济排序、进入详情和返回，结果仍为日本一行，并保留页面及横向位置。
- 在线打开四实体、三个农业字段分享链接；实际移除/重新添加肯尼亚、换能源主题并添加核能发电，已有三字段保留；独立公式参考折叠正常。
- 在线桌面能源表切换主题、搜索石油并自定义列；“仅保留实体”后刷新仍为一列，恢复完整原表后为 25 列。确认页面无横向溢出。
- 实际点击线上 XLSX 下载，浏览器收到下载事件；资源与下载正确使用 `/National-Power/` 子路径。浏览器未记录到 JavaScript error。
- 功能提交保持远端历史，以最新 main 为父提交，`force: false` 更新；没有覆盖其他提交或改变保护规则。

## 未测及限制

没有使用物理手机、Safari/Firefox 或屏幕阅读器进行验收；本轮手机结论指 Chromium 的 390 像素视口。未开展新的研究审计，不处理或重判已有军事编码、单位和公式问题。日常数据更新仍只替换 `data/source/national-power.xlsx`，原自动检查与部署流程保持不变。
